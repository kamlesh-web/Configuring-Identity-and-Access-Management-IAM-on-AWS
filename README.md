# Configuring-Identity-and-Access-Management-IAM-on-AWS
AWS Identity and Access Management (IAM) is a secure, global web service that controls who can access your AWS resources (authentication) and what actions they can perform (authorization). It enables creating users, groups, and roles, applying fine-grained policies to enforce least-privilege security.
